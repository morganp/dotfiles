
## Setting up RMagick on Webfaction
export LD_LIBRARY_PATH="$HOME/lib"
export PATH=$HOME/bin:$PATH

#Load standard ruby gem location if it exists
if [ -d $HOME/.gem/ruby/1.8/bin ]
then
  post_add_path $HOME/.gem/ruby/1.8/bin
fi

