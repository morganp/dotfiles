#!/bin/sh
brew update
brew upgrade
brew cleanup

#Tools Latest

brew install gh # Github command line
brew install gitx
brew install --cask macvim 
brew install --cask visual-studio-code
brew install tree

brew install python3
brew install vlc

brew install drawio


# Video encoding
brew install handbrake
brew install yt-dlp
brew install ffmpeg

#Bulk Renamer
brew install filebot

#Raspberry PI
brew install thonny



brew install kid3 #Uses rosetta 2 


# 2025 Additions
# NeoVIM (with verilog language)
# Neo vim setup is quite complicated there will be a seperat note
brew install neovim verible

# New config based terminal
brew install --cask alacritty


# for Python Course
brew install sqlite db-browser-for-sqlite

#2025 Removed
brew install bitwarden
brew install synology-drive
brew install iterm2
brew install lyx
brew install bambu-studio


# Old Tools Pre 2020
## Command Line apps
brew install rename
brew install transmission
# brew services start transmission
# For sctips to use notifications
brew install terminal-notifier
brew linkapps terminal-notifier

## GUI Apps
#brew tap phinze/cask
brew install brew-cask

brew cask update
brew cask install google-chrome

brew cask install macvim
#brew cask install adobe-creative-cloud
#brew cask install vlc ## Want 2.0.9 not the 2.1.x

brew cask install gitx
## Spyder screen callibration
brew cask install hugin
brew cask install graphsketcher
brew cask install iterm2
brew cask install omnidisksweeper
brew cask install flux
brew cask install max
brew cask install handbrake
brew cask install evernote
brew cask install dropbox
brew cask install little-snitch

## Install VNC (built in broken since Lion)
#brew install homebrew/x11/tiger-vnc
# Use web installer no X11 required

## Install VPN (get config from AirVPN)
brew cask install tunnelblick

brew cleanup

